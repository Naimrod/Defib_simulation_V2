version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    privileged: true
    volumes:
      - /dev:/dev
    ports:
      - "127.0.0.1:8000:8000" # Expose only to the host machine (Caddy)
